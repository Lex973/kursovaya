# Базовый URL API. По умолчанию (если не задан) используется Vite-прокси "/api",
# который перенаправляет запросы на Laravel (http://localhost:8000).
# Задавайте только если фронтенд и бэкенд работают на разных хостах.
# VITE_API_URL=http://localhost:8000/api
